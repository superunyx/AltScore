# Project Progress Tracker

## Latest Milestone: End-to-End On-Device Federated Learning Verified (August 29, 2026)
*   **Real On-Device Android Training:** Successfully executed TFLite local training loops end-to-end natively on physical Android hardware via `TFLiteModule.kt`. 
*   **Delta Extraction:** Wired the `export_weights` signature to correctly extract both `init_weights` and `tuned_weights` natively in memory.
*   **Format Matching:** Constructed a complex recursive JSON payload generator inside Kotlin to format the multidimensional tensor arrays into standard JSON arrays to perfectly match the existing Python simulation API contract.
*   **Native HTTP Upload:** Replaced the broken React Native binary upload with a native `OkHttp` JSON POST request that successfully sends the delta back to the FastAPI server without relying on the React Native bridge.
*   **Server-side Verification:** The FastAPI server successfully received 5 genuine Android-originated POSTs, triggered the `FedAvg` aggregation task, and correctly incremented the global model from version `11` to `12`.
*   **Inference Wiring:** Mapped the `infer` TFLite signature, computed the sigmoid raw score directly in C++ memory, converted it to a standard `[0, 1000]` range, and dynamically updated the React Native UI upon training completion.
*   **Documentation Refactor:** Completely rewrote `README.md` to focus on the human impact, the privacy-first mission, honest statistical results, and real-world execution for non-technical evaluation.

## Latest Milestone: Rigorous Statistical Validation & Dataset Integrity (September 1, 2026)
*   **Data Generation Audit & Patch:** Identified a bug in `generate_synthetic_data.py` that silently accumulated stale users across runs. Rewrote the logic to enforce a strictly clean, reproducible dataset of exactly 150 independent users (1,950 total windows).
*   **Generator Realism Fix:** Analyzed the feature mappings and discovered Income Stability (ISI) was nearly deterministic ($R^2=0.86$). Injected irreducible noise into the volatility and expense ratio mappings to ensure the synthetic dataset demands genuine, multi-feature learning (reducing ISI $R^2$ to a defensible 0.52).
*   **Statistical Pipeline Re-run:** Executed the entire pipeline front-to-back (pretraining, 3-seed federated evaluation, stats checks) on the newly vetted, noise-injected 150-user dataset to guarantee completely internally consistent results.
*   **Robust Statistical Documentation:** Updated the `README.md` to bulletproof standard. Added honest caveats explaining that the paired t-test ($t=0.14, p=0.905$) has limited power on 3 seeds, clarified the train vs. test mean discrepancy for the naive baseline (0.1931 MAE), and computed feature correlations entirely at the user-level ($n=150$) to avoid sliding-window pseudo-replication.
*   **Architecture Diagram:** Created a comprehensive `ARCHITECTURE.md` visually breaking down the system into 4 distinct privacy layers (Feature, Local FL, Differential Privacy, Network).
*   **Outstanding Blocker:** Physical Android device verification of the updated feature-extraction pipeline remains open.

## Latest Milestone: Phase 9 Complete, Real Native Extraction & Modern UI (September 3, 2026)
*   **Differential Privacy & Envelope Encryption:** Phase 9 is fully implemented. The Android client locally applies Gaussian noise and clips the gradient weights. The update is then encrypted with AES-256-GCM, and the symmetric key is wrapped using RSA-OAEP before transmission to the server.
*   **Real SMS & App Usage Extraction:** Removed dummy data fallbacks. The Android native `TFLiteModule.kt` now genuinely requests `READ_SMS` and Usage Access permissions. It processes raw Indian gig-worker banking SMS formats (Dr./Cr.) to natively compute the 8-ratio feature vector entirely on-device.
*   **Privacy Hygiene:** Verified that no plaintext SMS dumps, Python debug scripts, or user PII ever touched the git repository. The privacy-first architecture guarantees zero raw data leaves the physical device.
*   **Backend Admin Dashboard:** Upgraded FastAPI to serve an HTML/Chart.js dashboard via `StaticFiles`. The dashboard is secured via token and visualizes active clients, federated model versions, and explicit opt-in score reviews.
*   **Opt-In Score Sharing:** Deprecated the old plaintext `/score` endpoint. Built a new encrypted `POST /submit_score_for_review` endpoint. The mobile client wraps the calculated score in the same AES/RSA envelope, requiring explicit user consent (`consent_given=True`).
*   **Modern React Native UI:** Refactored the single-screen app into a multi-tab architecture (`Dashboard`, `History`, `Train`, `Privacy`, `Settings`) using `@react-navigation/bottom-tabs`. 
*   **UI Polish:** Transitioned from basic/neo-brutalism to a sleek Dark Mode glassmorphic design. Integrated `react-native-svg` to build a dynamic, mathematically proportionate circular score ring and customized vector icons for the tab bar navigation. Outlined the architectural native Android steps (Edge-to-Edge `WindowCompat` & SVG `<Text>`) required for flawless pixel-perfect UI rendering on all OEM devices.

## Latest Milestone: Premium UI Overhaul & App Asset Upgrade (September 4, 2026)
*   **Premium OLED Aesthetics:** Fully redesigned the app with a sleek, dark mode, trust-first fintech design. Upgraded components with high-elevation drop shadows, glassmorphic transparency, and high-contrast neon gradients.
*   **Performance Optimization:** Resolved severe tab-switching lag by moving the 500-star cosmic background SVG (`StarField`) to the root `App` level, rendering it only once under a transparent navigation container.
*   **Custom Liquid-Fill Intro Animation:** Built a stunning new app boot sequence. Replaced basic splash screens with a high-energy React Native liquid-fill text animation that shakes while loading. Engineered this using overlapping text layers and `overflow: hidden` to successfully bypass deep-rooted SVG `<ClipPath>` hardware acceleration bugs on Android.
*   **Streamlined Navigation:** Removed the bulky floating pill menu and old `Privacy` tab. The bottom menu is now a flush, semi-transparent glass pane, and privacy architecture is cleanly folded into an expandable section in `Settings`. Removed the clunky default Android ripple effect for a minimal opacity fade on touch.
*   **New Brand Assets:** Generated and integrated a brand-new, modern app icon (neon green/cyan hollow ring) to match the new UI. Updated native Expo and Android assets (`icon.png`, `splash-icon.png`, `android-icon-foreground.png`) so the icon accurately displays on physical device launchers after native builds.
*   **Git Sync:** Committed and pushed the entire UI overhaul to the remote repository.

## Latest Milestone: Dynamic LAN Networking & Standalone Release APK (September 6, 2026)
*   **Configurable Server URL:** Decoupled all hardcoded `127.0.0.1:8000` URLs across Kotlin (`TFLiteModule.kt`), React Native (`App.js`), and server components. Added a configurable server URL setting with persistence via `AsyncStorage` and a live "Test Connection" (`/health`) check in the mobile UI.
*   **Server LAN Discovery & Host Binding:** Configured FastAPI to bind to `0.0.0.0` by default (configurable via `HOST`/`PORT` environment variables) and automatically discover and print out the machine's local LAN IP (e.g., `http://192.168.1.4:8000`) on startup alongside the healthcheck endpoint.
*   **Cleartext Traffic Configuration:** Verified global cleartext support in `AndroidManifest.xml` (`android:usesCleartextTraffic="true"`) ensuring HTTP LAN traffic works without requiring TLS/HTTPS certificates on private subnets.
*   **Standalone Release Build Pipeline:** Generated a dedicated release keystore (`release.keystore`), wired `signingConfigs.release` into `mobile/android/app/build.gradle`, excluded keystores in `.gitignore`, and built the standalone signed release APK (`app-release.apk`) with minification disabled to safeguard reflection and JNI libraries.
*   **Hardware On-Device Standalone Verification:** Installed the release APK onto the physical Android smartphone and validated end-to-end functionality completely independent of Metro bundler or USB adb forwarding. Successfully performed on-device Test Connection, local TFLite training round, public key retrieval, encrypted gradient delta submission (`/submit_update`), and opt-in score review (`/submit_score_for_review`) over local Wi-Fi with live server logs confirming incoming requests from `192.168.1.7`.
